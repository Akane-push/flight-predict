apiVersion: v1
kind: Service
metadata:
  name: api-predict
  namespace: flight-project
spec:
  selector:
    app: api-predict
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 32000
  type: LoadBalancer